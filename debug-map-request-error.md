# [OPEN] map-request-error

## 现象
- 地图相关请求再次出现异常，当前用户反馈“关于地图的请求又出问题了”。

## 期望
- 地图页面能够正常发起并完成相关请求，地图加载、检索或附近搜索可正常工作。

## 当前上下文
- 需要优先确认是前端请求参数、前端代理转发、后端地图接口，还是外部地图服务异常。
- 本阶段只收集证据，不修改业务逻辑。

## 假设
- H1: 前端地图页面发出的请求 URL 或参数不对，导致请求在浏览器侧直接失败。
- H2: 前端开发代理或后端转发接口异常，导致请求没有正确到达外部地图服务。
- H3: 后端地图相关接口报错或超时，导致前端看到请求失败。
- H4: 外部地图服务返回了错误码，可能与 key、城市参数、搜索半径或调用配额有关。
- H5: 最近改动影响了地图页面初始化时机，导致请求触发顺序异常。

## 待收集证据
- 地图页面对应的前端请求入口和参数
- 前端是否走代理、代理目标是谁
- 后端地图请求入口、请求参数、异常日志
- 浏览器/接口返回的具体错误信息

## 已收集证据
- `amap env check` 日志显示 `VITE_AMAP_KEY` 和 `VITE_AMAP_SECURITY_JS_CODE` 都存在。
- `amap sdk script appended` 日志显示前端已尝试加载 `https://webapi.amap.com/maps?...`。
- 随后连续出现 `amap sdk script load failed`，说明失败点在浏览器加载高德 SDK 脚本本身，而不是后端接口。

## 当前结论
- 已排除“缺少地图环境变量”。
- 已排除“单个页面逻辑异常”，因为所有页面共用 `loadAmap()` 且都在同一外部脚本加载点失败。
- 高概率原因收敛为：网络无法访问 `webapi.amap.com`、浏览器/系统代理或安全软件拦截、或高德 Key 的域名白名单/安全策略不匹配。

## 状态
- evidence-collected

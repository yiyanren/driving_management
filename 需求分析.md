﻿# 3 需求分析

## 3.1 可行性分析

### 3.1.1 技术可行性分析

本系统定位为驾校运营管理平台，采用前后端分离架构：前端基于 Vue 3 + Vite + Ant Design Vue，后端基于 Spring Boot + Spring Security + JPA，数据库采用 MySQL。该技术路线具备以下可行性：

1. 前端采用组件化开发模式，结合 vue-router 与 Pinia，可高效实现多角色页面隔离、状态统一管理与复杂交互。
2. 后端采用 Spring Boot 自动配置与分层架构（Controller/Service/Repository），并引入 JWT + RBAC 完成认证授权，技术方案成熟。
3. 业务数据模型明确（意向客户、学员、课程计划、训练记录、考场、报考申请、操作日志），适合关系型数据库建模。
4. 地图模块可通过高德 JS SDK 快速接入路线规划能力；报表模块可通过 ECharts 实现可视化统计。
5. 系统已具备分页查询、统一响应体、操作日志记录等基础工程能力，后续扩展成本较低。

### 3.1.2 操作可行性分析

系统角色分为管理员、招生、教练、学员四类，界面按角色提供差异化菜单与权限控制。业务流程围绕“意向客户 -> 学员 -> 教学 -> 报考 -> 统计分析”展开，符合驾校日常运营逻辑。

1. 普通业务操作以表格查询、表单录入、状态流转为主，学习门槛低。
2. 页面提供输入校验、权限提示、错误反馈，支持常规办公场景。
3. 地图路线规划与图表报表可提升业务人员决策效率。
4. 系统支持主流浏览器访问，部署在 Windows/Linux 环境均可运行。

### 3.1.3 经济可行性分析

1. 选用开源技术栈（Vue、Spring Boot、MySQL、ECharts），无高额授权费用。
2. 平台化管理替代手工统计，降低人工录入错误与沟通成本。
3. 意向客户转化、教学进度、报考审核等核心流程实现线上化，可缩短业务处理周期。
4. 模块化设计支持后续增量建设（如 Excel 导出、消息通知、更细粒度权限），避免重复开发投入。

## 3.2 系统总体需求分析

### 3.2.1 适用对象需求分析

1. 管理员：负责账号权限、全局业务监督、关键报表查看、系统配置与审计。
2. 招生：负责意向客户管理、客户转学员、报考申请处理、转化结果跟踪。
3. 教练：负责课程计划安排、训练记录维护、学员学时进度跟踪。
4. 学员：主要查看个人相关信息（如地图路线、个人进度等受限功能）。

### 3.2.2 系统功能需求分析

（1）认证与权限管理模块

- 用户登录认证（JWT）。
- 基于角色的访问控制（管理员/招生/教练/学员）。
- 接口级权限拦截与前端路由级权限控制。

（2）意向客户管理模块

- 意向客户信息录入、编辑、分页查询。
- 状态管理（新建、跟进、转化、失效）。
- 意向客户转学员流程。
- 来源统计与转化业绩统计。

（3）学员管理模块

- 学员信息查询、编辑、删除。
- 按姓名/手机号/状态检索。
- 学员状态维护及关联业务展示。

（4）教学管理模块

- 课程计划创建与查询。
- 训练记录录入与查询。
- 学时进度计算与报考准备状态判断。
- 学员选择方式统一为“姓名 + 手机号”。

（5）考场与报考管理模块

- 考场信息维护（地址、经纬度、场次、容量）。
- 报考申请提交与审核。
- 考场容量校验（满员限制）。

（6）地图路线模块

- 地图 SDK 接入与考场点位展示。
- 当前定位与目标考场路线规划。
- 路径摘要（距离、时间）与导航步骤面板。

（7）报表与导出模块

- 总览报表可视化（数量 + 通过率）。
- 漏斗报表可视化（意向客户、学员、报考申请）。
- 条件筛选与 CSV 导出。

### 3.2.3 系统性能需求分析

（1）响应性能

1. 常规页面查询响应时间目标 <= 2 秒。
2. 普通新增/编辑/状态更新操作目标 <= 2 秒。
3. 报表查询在常规数据量下目标 <= 3 秒。

（2）安全性

1. 登录采用 JWT，接口需携带 Bearer Token。
2. 敏感操作按角色控制，越权请求必须拒绝。
3. 关键业务操作记录到操作日志，便于审计追踪。

（3）可用性与兼容性

1. 支持 Chrome、Edge 等主流浏览器。
2. 支持 1366x768 及以上主流分辨率办公场景。

（4）可扩展性

1. 前端按视图模块化组织，后端按领域服务分层。
2. 接口采用 REST 风格，便于后续对接第三方系统。
3. 地图、报表、导出能力可独立增强。

## 3.3 主要业务分析

### 3.3.1 用户登录与权限分流流程

1. 用户进入登录页输入账号密码。
2. 后端校验通过后返回 token 与角色信息。
3. 前端保存登录态，并按角色跳转默认首页：
   - 管理员/招生 -> 工作台
   - 教练 -> 教学管理
   - 学员 -> 地图页
4. 后续请求统一携带 token，未授权路由自动拦截。

### 3.3.2 意向客户转化流程

1. 招生录入意向客户信息并设置来源与负责人。
2. 在跟进过程中更新客户状态。
3. 满足转化条件后执行“转学员”，生成学员档案。
4. 转化结果进入报表统计，用于分析来源效果与人员业绩。

### 3.3.3 教学计划与训练闭环流程

1. 教练基于学员建立课程计划。
2. 训练完成后记录训练时长与备注。
3. 系统累计学时并计算是否达到报考条件。
4. 教学数据同步作为报考审批与运营决策依据。

### 3.3.4 报考申请与考场容量控制流程

1. 招生为学员提交报考申请（科目、考场、日期）。
2. 系统校验目标考场容量与预约数。
3. 审核通过后更新申请状态与考场预约数。
4. 业务人员可在报考列表追踪处理进度。

### 3.3.5 地图路线规划流程

1. 业务用户进入地图模块加载考场点位。
2. 系统定位当前位置（失败则降级为地图中心）。
3. 选择考场后执行驾车路线规划。
4. 页面展示路线详情、预计时长与距离，用于出行安排。

## 3.4 系统用例分析

### 3.4.1 顶层用例

- 管理员：用户与权限管理、全局数据查看、关键操作审计。
- 招生：意向客户管理、转学员、报考管理、报表查看。
- 教练：课程计划、训练记录、学时进度管理。
- 学员：个人相关信息浏览（受限能力）。

### 3.4.2 关键细化用例

（1）登录用例

- 参与者：全部角色。
- 前置条件：用户账号可用。
- 主流程：输入凭证 -> 后端校验 -> 返回 token -> 跳转角色首页。
- 后置条件：建立有效会话。

（2）意向客户转学员用例

- 参与者：招生、管理员。
- 前置条件：意向客户存在且状态可转化。
- 主流程：选择客户 -> 补充证件信息 -> 执行转化。
- 后置条件：生成学员档案，客户状态更新。

（3）创建训练记录用例

- 参与者：教练、管理员。
- 前置条件：学员存在。
- 主流程：选择学员（姓名+手机号）-> 填写训练信息 -> 保存记录。
- 后置条件：学时累计更新。

（4）提交报考申请用例

- 参与者：招生、管理员。
- 前置条件：学员存在，考场可预约。
- 主流程：选择学员与考场 -> 提交申请 -> 审核状态更新。
- 后置条件：报考记录入库。

## 3.5 系统 E-R 图分析

结合当前数据库设计，核心实体及关系如下：

1. `sys_user`：系统登录用户，描述账号、角色、启用状态。
2. `lead_info`：意向客户主数据，可转化为 `student`。
3. `student`：学员基础信息，被教学与报考模块引用。
4. `course_plan`：与 `student` 为多对一关系，记录课程安排。
5. `training_record`：与 `student` 为多对一关系，记录训练过程与学时。
6. `exam_site`：考场主数据，包含容量与地理信息。
7. `exam_application`：与 `student`、`exam_site` 形成多对一关系，记录报考行为。
8. `operation_log`：记录关键操作，支持审计追踪。

 E-R 图

## 3.6 系统数据流图

### 3.6.1 意向客户到学员的数据流

1. 招生录入意向客户到 `lead_info`。
2. 跟进状态更新后触发转化。
3. 转化成功写入 `student`，并回写客户状态。
4. 统计模块读取意向与转化数据生成报表。

### 3.6.2 教学与报考的数据流

1. 教学模块写入 `course_plan` 与 `training_record`。
2. 进度查询汇总学时，判断报考条件。
3. 报考模块写入 `exam_application` 并联动 `exam_site` 容量字段。
4. 报表模块读取 `student`、`exam_application` 输出可视化结果与导出文件。

两张数据流图：

-  意向客户转化数据流图。
-  教学-报考联动数据流图。

## 3.7 本章小结

本章围绕驾校运营管理平台给出了可行性分析、角色与功能需求、性能与安全需求，并对登录、转化、教学、报考、地图等关键业务流程进行了结构化描述。在此基础上，进一步梳理了实体关系与数据流向，为后续详细设计、测试设计与部署实施提供了明确的需求依据与边界。
